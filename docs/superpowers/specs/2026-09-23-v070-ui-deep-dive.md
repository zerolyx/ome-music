# v0.7.0 逐页对照重做：Folia / ECHO 精髓移植（UI 深潜）

日期：2026-9-23。目标：不再凭印象加功能，对照 `D:\Download\ome-reference\folia-major` 与 `D:\Download\ome-reference\echo` 逐视图抠细节。

## 硬约束（继承）

- 前端依赖仅 preact/@preact/signals/@tauri-apps/api；UI 文案简体中文；`--ease-signature`；prefers-reduced-motion。
- 每批收尾：tsc + eslint + vitest；Rust 未动可省。提交 Conventional Commits。

## 对照清单（按视觉收益排序）

### T1 首页电台区（Now Playing）
- folia pendolo：封面大图 + 旋转唱片动效（播放时转、暂停缓停）；无封面时碟面更有质感（高光/纹理）
- 标题/艺人：display 衬线大字阶 + 字距微调；kicker 与标题层级拉开
- 氛围：封面模糊背景 + 双团氛围光已有，补暗角收拢视线；歌词区与碟的纵向节奏
- 底部 chips（歌词舞台/视觉器）改为悬停有底色的 pill，与 folia 一致

### T2 播放条（PlayerBar）
- ECHO mini-player 思路：封面 48px 圆角、进度条 hover 加粗、音量条浮现
- 按钮 hover 圆形底色；播放键主按钮实心强调色

### T3 曲目列表（TrackList）
- 行高 56px、hover 整行底色 + 封面小图、播放中行左侧竖条指示、序号换声波图标位
- 喜欢红心 hover 放大

### T4 全局质感
- 滚动条细圆隐藏式；卡片圆角统一 14px；阴影层级 review
- 页面切换淡入上移（view transition，100ms）

### T5 功能补完（ECHO 移植）
- 迷你播放器（右下角悬浮小窗，切页不挡内容）
- 桌面歌词（Tauri 第二窗口，歌词信号跨窗同步——最重，最后做）
- 歌词匹配候选确认弹窗（匹配出多候选时列出歌名/歌手供点选）

## 已完成的批次记录

（每完成一批在此追加：批次号 / 提交 hash / 验收点）

- T1 `72afb65` 首页电台区：衬线大字阶 + 封面投影圆角
- T2/T3/T4 `ff942cb` 播放条质感 / 曲目行竖条指示 / 滚动条与页过渡
- T5a `b8401de` 迷你播放器悬浮窗
- T5b 歌词候选确认弹窗
- 桌面歌词（第二窗口）留远期：需歌词信号跨窗同步，单独立项
