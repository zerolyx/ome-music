This folder is used during release builds.

The Windows release workflow places the managed NetEase Cloud Music runtime here
before Tauri packages the installer. Do not commit runtime binaries or generated
node_modules content.
